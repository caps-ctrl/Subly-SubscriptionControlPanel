services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ai_subscription_manager
    ports:
      - "5432:5432"
    volumes:
      - asm_pgdata:/var/lib/postgresql/data

volumes:
  asm_pgdata:

